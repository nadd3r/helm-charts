apiVersion: v1
kind: Service
metadata:
  name: {{ include "sillytavern.fullname" . }}
  labels:
    {{- include "sillytavern.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sillytavern.selectorLabels" . | nindent 4 }}
